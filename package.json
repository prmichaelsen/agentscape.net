{
  "name": "agentscape.net",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy ./public --project-name agentscape-net --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^3.86.0"
  }
}
